"""S2 timing stand (ADR-0020, wave BF-2) — the ONLY wall-clock domain.

Smoke mode (R=1) is informational and never blocks locally (ADR-0020
gate policy §3); the full repeat mode (``--repeats N``) is the nightly
contour's input on a quiet isolated machine.
"""
