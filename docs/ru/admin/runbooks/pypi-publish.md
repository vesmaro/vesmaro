# Runbook: Публикация в PyPI

**🌐 Language / Язык:** [English](../../../en/admin/runbooks/pypi-publish.md) · Русский

Пайплайн первой публикации пакета `mnemos` в PyPI — issue #122,
ADR-0017 Фаза 0 (Distribution). GitHub Actions заблокирован по billing
(#117), поэтому весь пайплайн выполняется локально через
`scripts/pypi-publish.sh` (сиблинг `scripts/local-release.sh`, который
отвечает за container-образ + GitHub Release).

**Первая публикация и финальное имя пакета — решения владельца.** Имена
и версии на PyPI неизменяемы: опубликованную версию невозможно
перезалить или заменить, а имя проекта нельзя тихо перенести. Всё
ниже готовит и проверяет артефакты — сам `twine upload` это
осознанный, ручной шаг владельца.

## Имя пакета — доступность (проверено 2026-08-21)

Естественное имя `mnemos` на PyPI **занято**. Матрица доступности:

| Имя | Статус на PyPI | Чем занято |
| --- | --- | --- |
| `mnemos` | ❌ занято (v0.1.1) | "Memory for agentic AI" — Tyson Chan |
| `mnemos-memory` | ❌ занято (v0.6.0) | "Biomimetic memory architectures for LLMs" |
| `mnemos-memory-server` | ✅ свободно | — |
| `mnemos-server` | ✅ свободно | — |
| `mnemos-mcp` | ✅ свободно | — |
| `mnemos-ai` | ✅ свободно | — |
| `mnemos-agent-memory` | ✅ свободно | — |

Оба занятых имени — **AI-memory-проекты в той же доменной области**;
третье похожее имя максимизирует путаницу у пользователей, поэтому
fallback должен быть самоописательным, а не минимальным.

**Рекомендация: `mnemos-memory-server`** — прямо говорит, что это
(«сервер памяти mnemos»), совпадает с описанием проекта и однозначно
отличается от обоих занятых соседей. Альтернатива: `mnemos-server`
(короче, всё ещё ясно).

Как перепроверить (без авторизации):

```bash
# 404 = имя свободно, 200 = занято
curl -s -o /dev/null -w '%{http_code}\n' https://pypi.org/simple/<name>/

# чем занято имя
curl -s https://pypi.org/pypi/<name>/json | python3 -c \
  "import json,sys; i=json.load(sys.stdin)['info']; print(i['version'], '|', i['summary'])"
```

Оговорки:

- **Нормализация PEP 503** — `mnemos-memory-server`,
  `mnemos_memory_server` и `mnemos.memory.server` это ОДНО имя на PyPI.
  Проверять надо в нормализованной форме.
- **Фильтр против тайпсквоттинга** — PyPI отклоняет регистрацию имён,
  схожих до путаницы с популярными пакетами. Порог считается на стороне
  сервера; финальное подтверждение любого имени происходит только при
  первой загрузке. Если отклонили — берите следующий кандидат из
  матрицы выше.

**Смена имени** — однострочная правка (`name = "..."` в
`pyproject.toml`) и пересборка: `scripts/pypi-publish.sh` читает имя из
`pyproject.toml` и подстраивается автоматически (включая нормализацию
имени файла wheel). Делать это ДО первой публикации; после — имя
закреплено навсегда.

## Пайплайн — `scripts/pypi-publish.sh`

| Режим | Что делает |
| --- | --- |
| по умолчанию (check) | гейты G0–G4, сборка wheel/sdist, `twine check`, офлайн metadata-smoke |
| `--full-smoke` | дополнительно ставит wheel С зависимостями в одноразовый venv, запускает `mnemos --version` (нужен pypi.org) |
| `--publish` | все проверки, затем `twine upload` (нужны release-тег и учётные данные) |
| `--publish --full-smoke` | рекомендуемая комбинация перед загрузкой |
| `--i-own-name` | обязателен для загрузки, когда проект на PyPI уже существует (обновления ТОЛЬКО нашего проекта) |
| `--reuse-dist` | пропустить сборку, если в `dist/` уже лежат подходящие артефакты |
| `--dry-run` | напечатать все шаги, ничего не менять |

Алиас в Makefile: `make pypi-publish` (check-режим).

### Гейты

| Гейт | Проверяет | Жёсткий отказ? |
| --- | --- | --- |
| G0 | имя на PyPI: 404 = свободно (первая публикация); 200 + `--i-own-name` = обновление; целевая версия уже на PyPI = всегда ошибка | режим `--publish` |
| G1 | HEAD ровно на release-теге `vX.Y.Z` | режим `--publish` |
| G2 | версия тега == версия в `pyproject.toml` | режим `--publish` |
| G3 | версии в именах wheel + sdist == версия в `pyproject.toml` | режим `--publish` |
| G4 | версия установленного в smoke-venv пакета == версия в `pyproject.toml` | всегда (доказательство артефактом) |

G0 существует, потому что имя проекта на момент упаковки ещё не выбрано
(см. матрицу выше): случайный `--publish` под занятым именем чисто
падает ЕЩЁ ДО попытки загрузки.

## Процедура публикации (владелец)

Когда имя выбран и релиз нарезан (`release/X.Y.Z` → `main`, тег
`vX.Y.Z`, см. git-workflow runbook):

1. **Создать API-токен PyPI** — <https://pypi.org/manage/account/token/>.
   Для самой ПЕРВОЙ загрузки проекта ещё нет, поэтому токен должен быть
   account-scoped; сразу после первой публикации удалите его и
   выпустите project-scoped токен для всех последующих загрузок. Токен
   нельзя вставлять в файлы, чат или историю shell — экспортируйте его
   в моменте:

   ```bash
   export PYPI_TOKEN=pypi-...   # из UI PyPI, живёт только в этом shell
   ```

2. **Переключиться на тег и запустить пайплайн целиком**:

   ```bash
   git checkout vX.Y.Z
   scripts/pypi-publish.sh --publish --full-smoke
   # если (и ТОЛЬКО если) проект на PyPI уже существует и наш:
   scripts/pypi-publish.sh --publish --full-smoke --i-own-name
   ```

3. **Проверка после публикации** (из ЧИСТОГО venv, не dev-ового):

   ```bash
   pip index versions <final-name>            # наша версия должна появиться
   curl -s -o /dev/null -w '%{http_code}\n' https://pypi.org/simple/<final-name>/   # 200
   pip install <final-name> && mnemos --version
   ```

4. **Закрыть цикл** — обновить `docs/en/admin/runbooks/install.md` (+ RU
   зеркало) реальной командой `pip install`, упомянуть пакет в notes
   релиза, отметить критерий приёмки «pip install works from PyPI» из
   #122.

## Правила неизменяемости (жёсткие ограничения PyPI)

- **Никогда не перезаливать версию.** Единожды загруженное имя файла
  выгорает навсегда, даже если проект удалить. Ошибка в `vX.Y.Z` →
  поднимите версию до `vX.Y.Z+1` (или `X.Y.(Z+1)`) и загрузите фикс.
- **Никогда не удалять и не пересоздавать проект** ради «сброса» версий.
- Сломанный релиз **yank-ается** (`pypi.org/manage/project/...`), а не
  удаляется — yank-нутые версии всё ещё резолвятся для pinned-установок.
- Переименовать опубликованный проект невозможно — новое имя означает
  новый проект, а старый продолжит существовать.

## См. также

- `scripts/pypi-publish.sh --help` — режимы пайплайна и гейты
- `scripts/local-release.sh` — container-образ + GitHub Release
- [Runbook установки](install.md) — first-run операционный чеклист
- [Runbook CI/CD](ci-cd.md) — почему сборки идут локально (billing lock)
- Issue #122, ADR-0017 (docs/project/adr/0017-memory-system-evolution-roadmap.md)
