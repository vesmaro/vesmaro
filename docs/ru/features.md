# Карта функционала

**🌐 Language / Язык:** [English](../en/features.md) · Русский

> Mnemos — слой памяти для AI-агентов: одно подключение — и харнес получает
> долгосрочную память: структурированную, с поиском, защищённую контрактом —
> она переживает сессии, рестарты и сжатие контекста.

Одна страница, три честных секции: что работает из коробки, что частично,
что в плане. Статусы отражают кодовую базу v4.0.0 (карта владельца
2026-08-31, обновлена 2026-09-05: упаковка выпущена, бенчмарк-стенды готовы).

---

## Работает из коробки

Подключил — и есть. Ничего из таблицы не требует дополнительной проводки.

| Возможность | Что даёт |
|-------------|----------|
| **Универсальное подключение** | MCP-сервер (26 инструментов, stdio) + REST API — любой харнесс с поддержкой MCP подключается одной строкой. Подробнее: [mcp-tools.md](user/mcp-tools.md), [http-api.md](user/http-api.md) |
| **Готовые интеграции** | zcode, стандарт `~/.agents` (Claude Code, Codex, Continue, Qwen и др.), pi — через `mnemos integration`: универсальные таргеты развёртывания, однострочные MCP-пресеты, доктор мульти-харнесов (`mnemos doctor` проверяет регистрацию MCP в известных харнессах). См. [руководство по интеграции](user/integration-guide.md) |
| **Пакет скиллов** | 14+ скиллов памяти деплоятся в харнесы вместе с инструментами |
| **Гибкая память** | Гибридный поиск (полнотекстовый + векторный, слияние ранжирования), [теги-контракт](user/tag-contract.md), память по агентам и по проектам, [контекстный фильтр](user/context-filter.md) с профилями (code / docs / web / logs …), сжатие CCR — маркер в контексте, оригинал в памяти, экономия 70–90% токенов |
| **Динамическая сборка контекста** | `assemble_context`: конвейер поиск → сжатие → фильтр → скан секретов → выравнивание кэша → бюджет токенов, провенанс каждого блока |
| **Мост контекста харнесса** | `on_context_rewrite`: при сжатии истории харнессом оригинал сохраняется без потерь и доступен по требованию ([ADR-0018](../project/adr/0018-context-rewrite-ltm-bridge.md)) |
| **Хуки жизненного цикла** | `pre_llm_call` (впрыск контекста перед запросом модели), `on_session_start`, `post_tool_call` (авто-сжатие выводов инструментов) |
| **Модель публикации v3.0.0** | Запись видна сразу после сохранения; фоновая дообработка с бесшовной подменой; карантин опасного контента с нейтральной ретракцией ([ADR-0019](../project/adr/0019-optimistic-publication-async-refinement.md)) |
| **Автозащита** | Детекторы инъекций и секретов на входе и публикации; скан каждой выдачи; полный аудит с привязкой к записи |
| **Автоконвейер** | Фоновый обработчик: кластеризация, дедупликация, гейт качества, публикация |
| **Встроенная модель эмбеддингов** | `mnema-embed-v1` (~30 МБ int8 ONNX) поставляется внутри wheel — гибридный векторный поиск работает полностью офлайн, на CPU, без скачиваний и API-ключей |
| **Упаковка и доставка** | PyPI [`mnemos-memory-server`](https://pypi.org/project/mnemos-memory-server/) (wheel несёт integration-пак и модель), npm `pi-mnemos` + алиасы, GHCR-образ `ghcr.io/korrnals/mnemos`, однострочный скрипт-установщик, фреймворк бенчмарков S1–S4 в репозитории |

## Частично — ядро есть, полнота в работе

| Область | Статус |
|---------|--------|
| **Автономность (~90%)** | Хранилище, поиск, сборка контекста и защита работают сами. Дисциплина хуков в произвольном харнесе обеспечивается задеплоенными инструкциями и скиллами (мягкая автоматика), а не жёсткой проводкой. Жёсткая проводка есть у Hermes-адаптера и частично у zcode; универсальный механизм хуков для любого харнесса — в плане |
| **Дообработка «мозгом» (LLM)** | Конвейер работает; кластеризация, дедупликация и гейт качества — настоящие. Качественное обогащение текста — детерминированная заглушка; LLM-провайдер подключается в зарезервированную точку интерфейса (в плане) |

## В плане

| Фича | Объём |
|------|-------|
| **Граф памяти** | Связи записей, каскад цитирований ([ADR-0017](../project/adr/0017-memory-system-evolution-roadmap.md)) |
| **Федерация между устройствами** | Постоянный канал обмена; сейчас — пакетный обмен файлами ([ADR-0017](../project/adr/0017-memory-system-evolution-roadmap.md)) |
| **Мульти-принципал** | Память нескольких владельцев с изоляцией |

---

## Что почитать дальше

| Тема | Источник |
|------|----------|
| Контракт провайдера, конвейер поиска, граф памяти, дистрибуция | [ADR-0017](../project/adr/0017-memory-system-evolution-roadmap.md) |
| Context rewrite и мост в LTM (`on_context_rewrite`) | [ADR-0018](../project/adr/0018-context-rewrite-ltm-bridge.md) |
| Модель публикации v3.0.0 (оптимистичная публикация, асинхронная дообработка) | [ADR-0019](../project/adr/0019-optimistic-publication-async-refinement.md) |
| Фреймворк бенчмарков (S1–S4) | [ADR-0020](../project/adr/0020-benchmark-framework.md) |
| Подключение конкретного харнесса | [integration-guide.md](user/integration-guide.md) |
| Все ADR | [adr/](../project/adr/README.md) |

---

_Источник: карта функционала, утверждённая владельцем (2026-08-31), сверена с
кодовой базой v4.0.0 — 26 инструментов зарегистрировано в `src/mnemos/mcp_server.py`,
пакет скиллов в `integrations/skills/`, стадии конвейера в `src/mnemos/pipeline/`,
бенчмарк-стенды в `benchmarks/`. Обновлено 2026-09-05._

_Последнее обновление: 2026-09-05_
