apiVersion: v2
name: vesmaro
description: Vesmaro (Mnemos) — local-first memory & knowledge server for AI agents
type: application

# Chart version — bump on any template or default change (semver).
version: 0.1.0
# Version of the container image deployed by default. The published images
# live at ghcr.io/korrnals/mnemos (mnemos brand) until the 5.0.0 registry
# migration completes (ADR-0031 / GWS card #331, phase g) — after that,
# switch image.repository to ghcr.io/vesmaro/vesmaro.
appVersion: "4.3.0"
kubeVersion: ">=1.25.0-0"

home: https://github.com/vesmaro/vesmaro
sources:
  - https://github.com/vesmaro/vesmaro
keywords:
  - memory
  - ai-agents
  - mcp
  - knowledge-base
  - sqlite
maintainers:
  - name: abyss
    email: abyss@vesmaro.dev

annotations:
  artifacthub.io/license: Apache-2.0
  artifacthub.io/images: |
    - name: vesmaro
      image: ghcr.io/korrnals/mnemos:4.3.0
