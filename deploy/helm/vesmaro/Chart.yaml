apiVersion: v2
name: vesmaro
description: Vesmaro (Mnemos) — local-first memory & knowledge server for AI agents
type: application

# Chart version — bump on any template or default change (semver).
version: 0.1.1
# Version of the container image deployed by default, published at
# ghcr.io/vesmaro/vesmaro (backfilled in the 4.3.0 wave; the release
# pipeline moves here permanently in 5.0.0 phase-g, GWS card #331).
appVersion: "4.3.0"
kubeVersion: ">=1.25.0-0"

home: https://github.com/vesmaro/vesmaro
sources:
  - https://github.com/vesmaro/vesmaro
keywords:
  - memory
  - ai-agents
  - mcp
  - knowledge-base
  - sqlite
maintainers:
  - name: abyss
    email: abyss@vesmaro.dev

annotations:
  artifacthub.io/license: Apache-2.0
  artifacthub.io/images: |
    - name: vesmaro
      image: ghcr.io/vesmaro/vesmaro:4.3.0
