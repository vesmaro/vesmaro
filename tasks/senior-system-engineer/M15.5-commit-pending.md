# Task M15.5 — Commit pending changes через `feat(m15)` PR

> **Task ID**: SSE-M15.5
> **Specialist**: GCW Senior System Engineer
> **Priority**: P1 (после M15.1 + M15.2 + M15.3 + M15.4 завершены)
> **Status**: ⏳ pending assignment
> **Created**: 2026-06-15

---

## Goal

Закоммитить накопившиеся изменения в working tree (M15.1 + M15.2 + M15.3 + M15.4 + возможно security-patches) в `feat/m15-production-hardening` ветку через один PR в `main`.

## Background

В `git status` на 2026-06-15:
```
Modified:   README.md, pyproject.toml, src/mnemos/cli/main.py, src/mnemos/manager.py,
            src/mnemos/storage/sqlite_store.py, src/mnemos/storage/vault.py,
            tests/test_api.py
Untracked:  CHANGELOG.md, Makefile, bandit-report.json, docs/runbooks/,
            src/mnemos/cli/migrate.py, tests/test_migration.py, tests/test_security.py
```

После M15.1 (mypy fix), M15.2 (bandit clean), M15.3 (SQL refactor), M15.4 (docs reconcile) — все эти правки плюс новые нужно собрать в один PR.

## Acceptance criteria

- [ ] Создана feature-ветка `feat/m15-production-hardening`
- [ ] Все modified + untracked файлы добавлены в commit
- [ ] `make verify` зелёный (lint + typecheck + security + test)
- [ ] Conventional commits format: `feat(m15): <summary>`
- [ ] PR создан через `gh pr create` с полным body
- [ ] CHANGELOG.md обновлён под `[Unreleased]`
- [ ] `make coverage` показывает ≥ 80%

## Pre-commit checklist (СТРОГО)

Перед `git add .` запустить:

```bash
cd /var/home/abyss/LABs/AI/mnemos
source .venv/bin/activate

# 1. Lint
ruff check src/ tests/                     # MUST: 0 errors
ruff format --check src/ tests/            # MUST: clean

# 2. Type
mypy --strict src/mnemos/                  # MUST: 0 errors

# 3. Security (real, no skips)
bandit -r src/ -f json                     # MUST: 0 HIGH/MEDIUM
pip-audit                                  # review findings

# 4. Tests
pytest tests/ -q                           # MUST: 209+ passed
pytest --cov=src/mnemos --cov-fail-under=80  # MUST: ≥ 80%

# 5. Smoke
mnemos --help                              # CLI работает
mnemos stats                               # быстрый check
```

**Если хоть один gate красный — STOP, не коммитить**. Идти к Tech Lead с отчётом.

## Commit strategy (по conventional commits)

НЕ один большой commit. Разбить на логические:

```bash
git checkout -b feat/m15-production-hardening

# 1. Mypy fixes (если M15.1 сделано)
git add src/mnemos/pipeline/__init__.py src/mnemos/manager.py \
        src/mnemos/storage/vault.py src/mnemos/storage/sqlite_store.py \
        src/mnemos/storage/vector_store.py src/mnemos/embeddings/__init__.py \
        src/mnemos/pipeline/cluster.py src/mnemos/mcp_server.py \
        src/mnemos/policy/scheduler.py src/mnemos/policy/triggers.py \
        pyproject.toml
git commit -m "fix(m15): resolve 45 mypy --strict errors

- Add explicit re-exports in mnemos/pipeline/__init__.py
- Type-guard sqlite_store counts/queries returning dict[str, int]
- Narrow object types in vault.py via cast(dict[str, Any], ...)
- Refactor policy/scheduler.py to handle None job entries
- Enable warn_return_any in pyproject.toml

Refs: tasks/senior-system-engineer/M15.1-mypy-strict.md"

# 2. Bandit fixes (если M15.2 сделано)
git add src/mnemos/embeddings/__init__.py src/mnemos/manager.py \
        src/mnemos/storage/sqlite_store.py src/mnemos/storage/vector_store.py \
        pyproject.toml
git commit -m "fix(m15): remove bandit skips, resolve SQL injection and supply chain risks

- B608: refactor f-string SQL in update_fields to whitelisted dispatch
- B608: rewrite fts_search to use static SQL template
- B615: pin hf_hub_download revisions for embedding models
- B104: add # nosec comment for 0.0.0.0 binding in container context
- Remove [tool.bandit] skips from pyproject.toml

Refs: tasks/senior-security-engineer/M15.2-bandit-cleanup.md"

# 3. SQL refactor (если M15.3 сделано, может быть объединено с 2)
# git commit -m "refactor(m15): SQL injection-safe query builder for SQLiteStore"

# 4. Docs (если M15.4 сделано)
git add README.md CHANGELOG.md
git commit -m "docs(m15): reconcile README/CHANGELOG with actual M1-M14 status

- Correct README status: M1-M14 complete, M15 in progress
- Update CHANGELOG [Unreleased] with M15 changes
- Remove false claim of 209 tests passing when mypy fails

Refs: tasks/tech-writer/M15.4-docs-reconcile.md"

# 5. Migrations CLI + runbooks (untracked files)
git add src/mnemos/cli/migrate.py tests/test_migration.py \
        docs/runbooks/ Makefile
git commit -m "feat(m13): migration CLI + operational runbooks

- Add mnemos migrate-from-ai-brain CLI command
- Add runbooks: install, migrate, backup-restore, dependency-updates
- Add Makefile with verify, lint, test, security, coverage targets

Refs: PLAN.md §M13"

# 6. Security tests + bandit report
git add tests/test_security.py bandit-report.json
git commit -m "test(m9): security regression suite for SSRF + SQL injection

- Add tests/test_security.py covering URL validation, exception
  narrowing, SQL parameterization
- Add bandit-report.json as snapshot (regenerated by make security)"

# 7. API tests (modified tests/test_api.py)
git add tests/test_api.py
git commit -m "test(api): expand API coverage to 82%"

# 8. CLI entry (modified src/mnemos/cli/main.py)
git add src/mnemos/cli/main.py
git commit -m "feat(cli): add migrate subcommand registration"
```

## Push и PR

```bash
git push -u origin feat/m15-production-hardening

gh pr create --base main \
  --title "feat(m15): production hardening — mypy, bandit, coverage, docs reconcile" \
  --body "## What
- Resolve 45 mypy --strict errors across 9 files
- Remove bandit skips; fix B608/B615/B104 findings
- Reconcile README/CHANGELOG with real M1-M14 status
- Add migration CLI + runbooks (M13)
- Add security regression tests (M9)
- Enforce coverage ≥ 80% in make verify

## Why
- make verify currently FAILS on mypy (45 errors)
- bandit skips in pyproject.toml mask real security issues
- README claims 'production-ready' but M15 is not complete
- Uncommitted changes accumulate technical debt

## How to verify
\`\`\`bash
cd /var/home/abyss/LABs/AI/mnemos
source .venv/bin/activate
make verify           # all gates green
make coverage         # ≥ 80%
mnemos --help         # CLI works
pytest tests/ -q      # 209+ passed
\`\`\`

## Risks
- mypy type narrowing may change runtime behavior in edge cases
  (mitigation: all 209 tests must pass)
- B608 refactor in update_fields preserves API (mitigation: existing
  tests cover CRUD paths)
- Bandit B104 # nosec for 0.0.0.0 binding is acceptable in container
  context (mitigation: documented in manager.py)

## Related
- Closes: M15 production hardening
- Refs: tasks/AUDIT.md, tasks/tech-lead/TL-001-coordination.md"
```

## Review checklist для self-review (перед открытием PR)

- [ ] Каждый commit — atomic, по conventional commits
- [ ] `git log --oneline main..HEAD` — не больше 8-10 коммитов
- [ ] `git diff main..HEAD --stat` — разумный объём
- [ ] Нет sensitive data (по `sensitive-data.instructions.md`)
- [ ] Нет broad `except: pass` (по `lint-and-validate.instructions.md`)
- [ ] Все новые public API задокументированы
- [ ] CHANGELOG.md обновлён
- [ ] Нет debug print'ов
- [ ] Нет закомментированного кода

## Post-merge

После approve + merge:
1. Удалить feature ветку: `git branch -d feat/m15-production-hardening`
2. Удалить remote ветку: `git push origin --delete feat/m15-production-hardening`
3. Сообщить Tech Lead о завершении

## Out of scope

- ❌ Дальнейшие M-фазы (M16/M17/M18/M19 — отдельные ветки)
- ❌ Force-push в main (запрещено по destructive-actions.instructions.md)
- ❌ Merge без approve от code-reviewer (по git-workflow.instructions.md)

## Hand-off

Report back to `@GCW: Tech Lead` with:
- Ссылка на PR
- Результат `make verify` output
- Количество коммитов в ветке
- Любые блокеры
