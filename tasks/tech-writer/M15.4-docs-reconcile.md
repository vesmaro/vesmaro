# Task M15.4 — Reconcile docs with reality

> **Task ID**: TW-M15.4
> **Specialist**: GCW Tech Writer
> **Priority**: P1 (после M15.1 + M15.2 + M15.3)
> **Status**: ⏳ pending assignment
> **Created**: 2026-06-15
> **Source**: [tasks/AUDIT.md §3.1](../AUDIT.md)

---

## Goal

Привести документацию (README.md, CHANGELOG.md, ARCHITECTURE.md, repo memory) в соответствие с реальным состоянием кода.

## Background

Текущая документация утверждает:
- README.md: "M1–M15 complete — 209 tests passing, make verify green. Production-ready."
- CHANGELOG.md: полный список M1-M15 фич в Released
- repo memory `/memories/repo/mnemos-progress.md`: все M-фазы `⏳ pending`

**Реальность**:
- 209 tests passing ✅
- `make verify` ❌ — mypy fails (45 errors)
- M1-M14 functional but M15 NOT done
- Security (M9) incomplete — bandit skips mask real issues
- Working tree dirty (7 modified + 7 untracked)

## Acceptance criteria

- [ ] README.md status section отражает реальный M1-M14 + M15-in-progress
- [ ] CHANGELOG.md `[Unreleased]` содержит M15 изменения корректно
- [ ] ARCHITECTURE.md § "M-series" обновлена если нужно
- [ ] Repo memory `/memories/repo/mnemos-progress.md` обновлена до актуального состояния
- [ ] Все ссылки в README работают (нет битых ссылок)
- [ ] Все упоминания "production-ready" убраны или контекстуализированы
- [ ] M16 (A2A Sessions API) отражена в roadmap (новая фаза)
- [ ] Markdown lint clean (по `docs-sync-runtime.instructions.md` §Step 4)

## Конкретные правки

### README.md

**Было**:
```markdown
**Status**: **M1–M15 complete** — 209 tests passing, `make verify` green. Production-ready.
```

**Стало**:
```markdown
**Status**: **M1–M14 complete, M15 (production hardening) in progress** — 209+ tests passing.
`make verify` is currently red (mypy strict 45 errors, bandit skips). See [tasks/AUDIT.md](tasks/AUDIT.md) for current state.
```

**Было** (milestones table):
```markdown
| M15 — Production Hardening | ✅ | — |
```

**Стало**:
```markdown
| M15 — Production Hardening | 🔄 in progress | — |
| M16 — A2A Sessions API (GCW v0.6.0) | ⏳ planned | — |
| M17 — CI pipeline | ⏳ planned | — |
```

**Было** (Next action):
```markdown
M15 — Production hardening: bandit, mypy, pip-audit, coverage ≥80%.
```

**Стало**:
```markdown
M15.1 (mypy strict) → M15.2 (bandit cleanup) → M15.3 (SQL refactor) → M15.4 (docs reconcile) → M15.5 (commit) → M19 (final review).
M16 (A2A Sessions API) running in parallel for GCW v0.6.0.
See [tasks/tech-lead/TL-001-coordination.md](tasks/tech-lead/TL-001-coordination.md).
```

### CHANGELOG.md

**Было** (в `## [0.1.0]`):
```markdown
- **M15**: Production hardening — Makefile with `make verify` (lint + typecheck + security + test).
```

**Стало** (split — released vs unreleased):
```markdown
## [0.1.0] — 2026-05-31 (released)

[existing M1-M14 entries]

## [Unreleased] — 2026-06-15

### In progress (M15)
- **M15.1**: Fix 45 mypy --strict errors (CRITICAL, blocks `make verify`)
- **M15.2**: Remove bandit skips, resolve B608/B615/B104 findings (CRITICAL)
- **M15.3**: Refactor f-string SQL to whitelisted dispatch (CRITICAL)
- **M15.4**: Reconcile documentation with actual state
- **M15.5**: Commit pending working tree via `feat/m15-production-hardening` PR

### Planned (M16+)
- **M16**: A2A Sessions API for GCW v0.6.0 (`/v1/sessions/*`, 5 endpoints)
- **M17**: CI pipeline (GitHub Actions / GitLab CI) with same gates as `make verify`
- **M18**: E2E smoke test via MCP client from real Copilot session
- **M19**: Final code review + merge to main

### Security
- SSRF guard `_validate_url()` blocking localhost, private IPs, AWS metadata endpoint (added uncommitted)
- Exception handling narrowed in `vault.py` and `sqlite_store.py` (added uncommitted)
```

### ARCHITECTURE.md

**Найти и обновить**:
- § "M-series milestones" если есть — обновить статусы
- § "Production readiness" если есть — отметить M15 incomplete
- § "What's NOT yet done" (если нет — создать) — список gaps

**Добавить новую секцию** если её нет:
```markdown
## 12. A2A Sessions API (M16 — planned)

GCW A2A routing требует 5 endpoint'ов для persistent backend (вместо file-fallback):
- `POST /v1/sessions`
- `GET /v1/sessions/{id}`
- `POST /v1/sessions/{id}/turns`
- `GET /v1/sessions/{id}/turns/{turn_id}?mode=summary|full`
- `POST /v1/sessions/{id}/turns/range`

Specification: `/var/home/abyss/LABs/Projects/Reserching/GithubCopilotWorkflow/docs/a2a/mnemos-requirements.md`
Status: ⏳ planned for GCW v0.6.0.
```

### Repo memory `/memories/repo/mnemos-progress.md`

**Было** (устаревший снэпшот, все M-фазы `⏳ pending`):
```markdown
### Next Steps (M-series milestones pending)
- ⏳ M1: git bootstrap
- ⏳ M3: Write real tests in tests/test_agent_recall.py
...
```

**Стало** (актуальный статус):
```markdown
## Status (✅ = done, 🔄 = in-progress, ⏳ pending) — 2026-06-15

### M-series
- ✅ M1: Fork & rebrand (verified via git log)
- ✅ M2: Tag contract (31/31 tests)
- ✅ M3: Agent recall (16/16 tests)
- ✅ M4: Knowledge pipeline (24/24 tests)
- ✅ M5: Policy engine (24/24 tests)
- ✅ M6: Traces (covered in test_traces_compaction)
- ✅ M7: Compaction detection (covered in test_traces_compaction)
- ✅ M8: Path-scoped rules (11/11 tests)
- ⚠️ M9: Security audit (SSRF guard added, but bandit skips mask 7+ findings)
- ✅ M10: Context filter (32/32 tests)
- ⏳ M11: Cache center (v2 deferred)
- ✅ M12: Docs + runbooks
- ✅ M13: Migration CLI (6/6 tests)
- ✅ M14: ai-brain archival
- 🔄 M15: Production hardening (mypy 45 errors, bandit skips — BLOCKED)
- ⏳ M16: A2A Sessions API (GCW v0.6.0 requirement)

### Verification status
- `pytest tests/ -q` → 209 passed ✅
- `ruff check src/ tests/` → 0 errors ✅
- `mypy --strict src/mnemos/` → 45 errors ❌
- `bandit -r src/` (with skips) → 0 ✅ (false sense of security)
- `bandit -r src/` (real) → 3 HIGH + 7 MEDIUM + 1 LOW ❌
- `make verify` → FAIL (mypy step)

### Working tree (uncommitted as of 2026-06-15)
- Modified: README.md, pyproject.toml, src/mnemos/cli/main.py, src/mnemos/manager.py,
  src/mnemos/storage/sqlite_store.py, src/mnemos/storage/vault.py, tests/test_api.py
- Untracked: CHANGELOG.md, Makefile, bandit-report.json, docs/runbooks/,
  src/mnemos/cli/migrate.py, tests/test_migration.py, tests/test_security.py
```

## Markdown lint gate (по `docs-sync-runtime.instructions.md` §Step 4)

Перед финализацией:
- [ ] No duplicate headings (MD024)
- [ ] Fenced code blocks specify language (MD040) — ` ```python ` not bare ` ``` `
- [ ] Consistent pipe spacing in tables (MD060)
- [ ] No trailing whitespace
- [ ] Consistent list markers (`-` not `*`)

Если есть `markdownlint` в проекте — запустить `markdownlint README.md CHANGELOG.md docs/**/*.md`.

## Files to touch

| Файл | Действие |
|---|---|
| `README.md` | edit — status section, milestones, next action |
| `CHANGELOG.md` | edit — split Released vs Unreleased, add M15 entries |
| `ARCHITECTURE.md` | edit — M-series section + A2A section |
| `docs/runbooks/*.md` | review — no broken references |
| `/memories/repo/mnemos-progress.md` | rewrite — actual state |
| `tasks/AUDIT.md` | already exists — review cross-references |

## Verification

```bash
cd /var/home/abyss/LABs/AI/mnemos

# 1. Lint markdown
markdownlint README.md CHANGELOG.md docs/  # if installed

# 2. Cross-reference check
grep -E "tasks/" README.md CHANGELOG.md | head -20
# Verify all task/* paths exist

# 3. Read final docs
cat README.md | head -50
cat CHANGELOG.md | head -50
```

## Commit strategy

Один commit `docs(m15): reconcile documentation with actual M1-M14 + M15-in-progress status`.

## Out of scope

- ❌ Переписывать всю архитектуру ARCHITECTURE.md
- ❌ Создавать новые runbooks (они уже есть)
- ❌ Переводить на русский (по `communication-language.instructions.md` — английский для docs)
- ❌ API reference generation (отдельная фаза, не M15)

## Hand-off

Report back to `@GCW: Tech Lead` with:
- Diff highlights
- Список обновлённых файлов
- Результат markdownlint (если есть)
- Любые неточности в PLAN.md / ARCHITECTURE.md, которые заметили по ходу

## Coordination

- Зависит от M15.1+M15.2+M15.3 (чтобы знать точный итоговый статус)
- После M15.5 (commit) — финальный polish если нужно
