# node.env — secrets for the core face unit (EnvironmentFile).
# Installed mode 0600 by vesmaro-node. Fill what you need; every line
# is optional — the core face on loopback needs none of them by default.

# Example overrides (VESMARO_ prefix maps onto config sections):
#VESMARO_API__AUTH_ENABLED=false
#VESMARO_LOGGING__LEVEL=INFO
